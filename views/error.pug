extends _layout

block content
    main#main
        i.fa.fa-error error
        if error
            h1= error.message
            pre= error.stack
        else
            h1= message
            p We're sorry, but something went wrong